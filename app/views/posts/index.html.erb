<h2><%= @user.name %>'s Posts</h2>

<ul>
<% @posts.each do |post| %>
  <li><%= post.body %>  <%= link_to "Edit post", edit_post_url(post) %>
    <% post.links.each do |link| %>
      <li style="padding-left:10px"><%= link_to link.url, link.url %></li>
    <% end %>
  </li>
<% end %>
</ul>